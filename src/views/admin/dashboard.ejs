<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Dashboard</title>

    <style>
      body {
        margin: 0;
        font-family: Arial, sans-serif;
        background: #fafafa;
      }

      .wrapper {
        display: flex;
        height: 100vh;
      }

      .main {
        flex: 1;
        padding: 25px 40px;
      }

      .dashboard-title {
        font-size: 22px;
        font-weight: bold;
        margin-bottom: 25px;
      }

      .stats-grid {
        display: grid;
        grid-template-columns: repeat(2, 260px);
        gap: 22px;
      }

      .stat-card {
        background: white;
        border-radius: 10px;
        padding: 22px;
        border: 1px solid #e5e5e5;
      }

      .stat-label {
        font-size: 14px;
        color: #444;
      }

      .stat-value {
        font-size: 32px;
        font-weight: bold;
        margin-top: 8px;
        color: #0c2b33;
      }

      .c-blue {
        background: #eaf2ff;
      }
      .c-green {
        background: #e8f7e8;
      }
      .c-peach {
        background: #ffeede;
      }
      .c-purple {
        background: #f5e8ff;
      }

      footer {
        text-align: center;
        padding: 15px;
        font-size: 14px;
        color: #777;
        position: absolute;
        bottom: 0;
        width: 100%;
      }
    </style>
  </head>

  <body>
    <div class="wrapper">
      <%- include('partials/sideBar') %>

      <div class="main">
        <div
          style="
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
          "
        >
          <h2 class="dashboard-title" style="margin: 0">Dashboard Overview</h2>

          <form action="/admin/logout" method="POST">
            <button
              style="
                padding: 10px 18px;
                background: #d9534f;
                color: white;
                border: none;
                border-radius: 6px;
                cursor: pointer;
                font-size: 14px;
                font-weight: 600;
              "
            >
              Logout
            </button>
          </form>
        </div>
        <div class="stats-grid">
          <div class="stat-card c-blue">
            <p class="stat-label">Total Departments</p>
            <p class="stat-value"><%= departmentCount%></p>
          </div>

          <div class="stat-card c-green">
            <p class="stat-label">Total Students</p>
            <p class="stat-value"><%= studentCount%></p>
          </div>

          <div class="stat-card c-peach">
            <p class="stat-label">Total Professors</p>
            <p class="stat-value"><%= professorCount%></p>
          </div>

          <div class="stat-card c-purple">
            <p class="stat-label">Total HODs</p>
            <p class="stat-value"><%= hodCount%></p>
          </div>
        </div>
      </div>
    </div>

    <footer>© 2025 Admin Dashboard. All rights reserved.</footer>
  </body>
</html>

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Upload Assignment - AssignmentChecker</title>
    <style>
      :root {
        --radius: 0.65rem;
        --background: oklch(1 0 0);
        --foreground: oklch(0.141 0.005 285.823);
        --card: oklch(1 0 0);
        --card-foreground: oklch(0.141 0.005 285.823);
        --primary: oklch(0.541 0.281 293.009);
        --primary-foreground: oklch(0.969 0.016 293.756);
        --secondary: oklch(0.967 0.001 286.375);
        --secondary-foreground: oklch(0.21 0.006 285.885);
        --muted: oklch(0.967 0.001 286.375);
        --muted-foreground: oklch(0.552 0.016 285.938);
        --destructive: oklch(0.577 0.245 27.325);
        --border: oklch(0.92 0.004 286.32);
        --input: oklch(0.92 0.004 286.32);

        /* ADD — this must match your sidebar width */
        --sidebar-width: 240px;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
          Oxygen, Ubuntu, Cantarell, sans-serif;
        background: var(--background);
        color: var(--foreground);
        line-height: 1.6;
        display: flex;
      }

      /* -----------------------------------------------
     FIX: Push content to right of sidebar
     ----------------------------------------------- */
      .container {
        width: calc(100% - var(--sidebar-width));
        margin-left: var(--sidebar-width);
        padding: 40px 32px;
      }

      /* Header */
      .page-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 40px;
        gap: 20px;
      }

      .page-header h1 {
        font-size: 32px;
        font-weight: 700;
        color: var(--foreground);
        margin: 0;
      }

      .back-link {
        color: var(--primary);
        text-decoration: none;
        font-weight: 600;
        font-size: 14px;
        transition: all 0.2s ease;
      }

      .back-link:hover {
        opacity: 0.8;
      }

      /* Card */
      .form-card {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        padding: 32px;
        max-width: 900px;
      }

      .form-group {
        display: flex;
        flex-direction: column;
        margin-bottom: 24px;
      }

      label {
        font-weight: 600;
        font-size: 14px;
        color: var(--foreground);
        margin-bottom: 8px;
        display: block;
      }

      .form-hint {
        font-size: 12px;
        color: var(--muted-foreground);
        margin-top: 4px;
      }

      /* Inputs */
      input[type='text'],
      input[type='file'],
      select,
      textarea {
        width: 100%;
        padding: 12px 16px;
        border: 1px solid var(--border);
        border-radius: var(--radius);
        font-size: 14px;
        background: var(--card);
        color: var(--foreground);
        font-family: inherit;
        transition: all 0.2s ease;
      }

      input[type='text']::placeholder,
      textarea::placeholder {
        color: var(--muted-foreground);
      }

      input[type='text']:focus,
      input[type='file']:focus,
      select:focus,
      textarea:focus {
        outline: none;
        border-color: var(--primary);
        box-shadow: 0 0 0 3px rgba(84, 28, 145, 0.12);
      }

      textarea {
        min-height: 120px;
        resize: vertical;
      }

      /* Row */
      .form-row {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 20px;
      }

      .form-full {
        grid-column: 1/-1;
      }

      /* File Input */
      .file-input-wrapper {
        position: relative;
        overflow: hidden;
      }

      .file-input-label {
        display: inline-block;
        padding: 12px 16px;
        background: var(--secondary);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        cursor: pointer;
        font-weight: 600;
        font-size: 14px;
        color: var(--secondary-foreground);
        transition: all 0.2s ease;
        text-align: center;
      }

      .file-input-label:hover {
        background: var(--muted);
      }

      input[type='file'] {
        display: none;
      }

      .file-name {
        display: block;
        font-size: 13px;
        color: var(--muted-foreground);
        margin-top: 8px;
      }

      /* Buttons */
      .form-actions {
        display: flex;
        gap: 12px;
        margin-top: 32px;
      }

      .btn {
        flex: 1;
        padding: 12px 24px;
        border-radius: var(--radius);
        border: none;
        font-weight: 600;
        font-size: 14px;
        cursor: pointer;
        transition: all 0.2s ease;
        text-align: center;
      }

      .btn-primary {
        background: var(--primary);
        color: var(--primary-foreground);
      }
      .btn-primary:hover {
        opacity: 0.9;
        transform: translateY(-1px);
      }

      .btn-secondary {
        background: var(--secondary);
        color: var(--secondary-foreground);
        border: 1px solid var(--border);
      }
      .btn-secondary:hover {
        background: var(--muted);
      }

      .required {
        color: var(--destructive);
        font-weight: 700;
      }

      /* Responsive Fix for Sidebar Layout */
      @media (max-width: 900px) {
        .container {
          margin-left: 0;
          width: 100%;
          padding: 24px;
        }

        body {
          flex-direction: column;
        }
      }

      @media (max-width: 768px) {
        .form-row {
          grid-template-columns: 1fr;
        }

        .form-card {
          padding: 24px;
        }
      }

      @media (max-width: 480px) {
        .container {
          padding: 16px;
        }

        .page-header h1 {
          font-size: 22px;
        }
      }
    </style>
  </head>
  <body>
    <%- include('_sidebar') %>
    <div class="container">
      <div class="page-header">
        <h1>Upload Assignment</h1>
      </div>

      <div class="form-card">
        <form method="POST" enctype="multipart/form-data">
          <div class="form-group">
            <label for="title"
              >Assignment Title <span class="required">*</span></label
            >
            <input
              type="text"
              id="title"
              name="title"
              placeholder="e.g., Data Analysis Report"
              required
            />
          </div>

          <div class="form-group">
            <label for="description"
              >Description <span class="required">*</span></label
            >
            <textarea
              id="description"
              name="description"
              placeholder="Provide a brief description of your assignment..."
              required
            ></textarea>
            <div class="form-hint">
              Include key information, methodology, or relevant notes for the
              reviewer.
            </div>
          </div>

          <div class="form-row">
            <div class="form-group">
              <label for="category"
                >Category <span class="required">*</span></label
              >
              <select id="category" name="category" required>
                <option value="">-- Select category --</option>
                <option value="assignment">Assignment</option>
                <option value="project">Project</option>
                <option value="report">Report</option>
                <option value="research">Research</option>
                <option value="other">Other</option>
              </select>
            </div>

            <div class="form-group">
              <label for="faculty"
                >Faculty Reviewer <span class="required">*</span></label
              >
              <select id="faculty" name="faculty" required>
                <option value="">-- Select faculty --</option>
                <% if (Array.isArray(faculty)) { %> <%
                faculty.forEach(function(f){ %>
                <option value="<%= f._id %>"><%= f.name %></option>
                <% }); %> <% } %>
              </select>
              <div class="form-hint">
                Select a professor from your department.
              </div>
            </div>
          </div>

          <div class="form-group form-full">
            <label for="files"
              >Upload Files <span class="required">*</span></label
            >
            <div class="file-input-wrapper">
              <label class="file-input-label" for="files"
                >Choose Files (up to 5 PDFs)</label
              >
              <input
                type="file"
                id="files"
                name="files"
                multiple
                accept=".pdf"
                required
              />
              <div class="file-name">Only PDF files are allowed</div>
            </div>
          </div>

          <div class="form-actions">
            <a href="/student/assignments" class="btn btn-secondary">Cancel</a>
            <button type="submit" class="btn btn-primary">
              Upload Assignment
            </button>
          </div>
        </form>
      </div>
    </div>

    <script>
      document.getElementById('files').addEventListener('change', function (e) {
        const fileName = document.querySelector('.file-name')
        if (e.target.files.length > 0) {
          fileName.textContent = `${e.target.files.length} file(s) selected`
        }
      })
    </script>
  </body>
</html>

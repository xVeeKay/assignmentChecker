<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Resubmit Assignment - AssignmentChecker</title>
    <style>
      :root {
        --radius: 0.65rem;
        --background: oklch(1 0 0);
        --foreground: oklch(0.141 0.005 285.823);
        --card: oklch(1 0 0);
        --card-foreground: oklch(0.141 0.005 285.823);
        --primary: oklch(0.541 0.281 293.009);
        --primary-foreground: oklch(0.969 0.016 293.756);
        --secondary: oklch(0.967 0.001 286.375);
        --secondary-foreground: oklch(0.21 0.006 285.885);
        --muted: oklch(0.967 0.001 286.375);
        --muted-foreground: oklch(0.552 0.016 285.938);
        --destructive: oklch(0.577 0.245 27.325);
        --border: oklch(0.92 0.004 286.32);
        --input: oklch(0.92 0.004 286.32);
        --chart-5: oklch(0.432 0.232 292.759);
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
          Oxygen, Ubuntu, Cantarell, sans-serif;
        background: var(--background);
        color: var(--foreground);
        line-height: 1.6;
      }

      .container {
        max-width: 700px;
        margin: 0 auto;
        padding: 40px 32px;
      }

      .page-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 40px;
        gap: 20px;
      }

      .page-header h1 {
        font-size: 32px;
        font-weight: 700;
        color: var(--foreground);
        margin: 0;
      }

      .back-link {
        color: var(--primary);
        text-decoration: none;
        font-weight: 600;
        font-size: 14px;
        transition: all 0.2s ease;
      }

      .back-link:hover {
        color: var(--chart-5);
      }

      .alert {
        background: var(--chart-5);
        border: 1px solid var(--chart-5);
        border-radius: var(--radius);
        padding: 16px;
        margin-bottom: 24px;
        color: var(--foreground);
      }

      .alert-title {
        font-weight: 600;
        margin-bottom: 8px;
      }

      .alert-message {
        font-size: 14px;
        line-height: 1.6;
        white-space: pre-wrap;
        word-break: break-word;
      }

      .form-card {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        padding: 32px;
      }

      .form-group {
        display: flex;
        flex-direction: column;
        margin-bottom: 24px;
      }

      label {
        font-weight: 600;
        font-size: 14px;
        color: var(--foreground);
        margin-bottom: 8px;
        display: block;
      }

      .form-hint {
        font-size: 12px;
        color: var(--muted-foreground);
        margin-top: 4px;
      }

      textarea,
      input[type='file'] {
        width: 100%;
        padding: 12px 16px;
        border: 1px solid var(--border);
        border-radius: var(--radius);
        font-size: 14px;
        background: var(--card);
        color: var(--foreground);
        font-family: inherit;
        transition: all 0.2s ease;
      }

      textarea::placeholder,
      input[type='file']::placeholder {
        color: var(--muted-foreground);
      }

      textarea:focus,
      input[type='file']:focus {
        outline: none;
        border-color: var(--primary);
        box-shadow: 0 0 0 3px rgba(84, 28, 145, 0.1);
      }

      textarea {
        min-height: 100px;
        resize: vertical;
      }

      .file-input-wrapper {
        position: relative;
        overflow: hidden;
      }

      .file-input-label {
        display: inline-block;
        padding: 12px 16px;
        background: var(--secondary);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        cursor: pointer;
        font-weight: 600;
        font-size: 14px;
        color: var(--secondary-foreground);
        transition: all 0.2s ease;
        text-align: center;
      }

      .file-input-label:hover {
        background: var(--muted);
      }

      input[type='file'] {
        display: none;
      }

      .file-name {
        display: block;
        font-size: 13px;
        color: var(--muted-foreground);
        margin-top: 8px;
      }

      .form-actions {
        display: flex;
        gap: 12px;
        margin-top: 32px;
      }

      .btn {
        flex: 1;
        padding: 12px 24px;
        border-radius: var(--radius);
        border: none;
        font-weight: 600;
        font-size: 14px;
        cursor: pointer;
        transition: all 0.2s ease;
      }

      .btn-primary {
        background: var(--primary);
        color: var(--primary-foreground);
      }

      .btn-primary:hover {
        opacity: 0.9;
        transform: translateY(-1px);
      }

      .btn-secondary {
        background: var(--secondary);
        color: var(--secondary-foreground);
        border: 1px solid var(--border);
      }

      .btn-secondary:hover {
        background: var(--muted);
      }

      .required {
        color: var(--destructive);
        font-weight: 700;
      }

      @media (max-width: 1024px) {
        .container {
          max-width: 100%;
          padding: 32px 24px;
        }
      }

      @media (max-width: 768px) {
        .container {
          padding: 20px 16px;
        }

        .page-header {
          flex-direction: column;
          align-items: flex-start;
        }

        .page-header h1 {
          font-size: 24px;
        }

        .back-link {
          font-size: 13px;
        }

        .alert {
          padding: 12px;
          margin-bottom: 20px;
        }

        .alert-title {
          font-size: 13px;
        }

        .alert-message {
          font-size: 12px;
        }

        .form-card {
          padding: 20px;
        }

        .form-actions {
          flex-direction: column;
        }

        .btn {
          width: 100%;
        }

        label {
          font-size: 13px;
        }

        textarea,
        input[type='file'] {
          font-size: 16px;
        }
      }

      @media (max-width: 480px) {
        .container {
          padding: 16px 12px;
        }

        .page-header {
          gap: 12px;
        }

        .page-header h1 {
          font-size: 20px;
        }

        .back-link {
          font-size: 12px;
        }

        .alert {
          padding: 12px;
          margin-bottom: 16px;
        }

        .alert-title {
          font-size: 12px;
          margin-bottom: 6px;
        }

        .alert-message {
          font-size: 11px;
          line-height: 1.5;
        }

        .form-card {
          padding: 16px;
        }

        .form-group {
          margin-bottom: 16px;
        }

        label {
          font-size: 12px;
          margin-bottom: 6px;
        }

        .form-hint {
          font-size: 10px;
        }

        textarea {
          min-height: 80px;
          padding: 10px 12px;
        }

        .file-input-label {
          padding: 10px 12px;
          font-size: 12px;
        }

        .file-name {
          font-size: 11px;
        }

        .btn {
          padding: 10px 16px;
          font-size: 13px;
        }
      }
    </style>
  </head>
  <body>
    <%- include('_sidebar') %>
    <div class="container">
      <div class="page-header">
        <h1>Resubmit Assignment</h1>
      </div>

      <% if (assignment) { %> <% if (remarks) { %>
      <div class="alert">
        <div class="alert-title">Reviewer's Feedback</div>
        <div class="alert-message"><%= remarks %></div>
      </div>
      <% } %>

      <div class="form-card">
        <form method="POST" enctype="multipart/form-data">
          <div class="form-group">
            <label for="description"
              >Revision Notes <span class="required">*</span></label
            >
            <textarea
              id="description"
              name="description"
              placeholder="Explain what you've changed and improved..."
              required
            ></textarea>
            <div class="form-hint">
              Describe the changes you made based on the reviewer's feedback.
            </div>
          </div>

          <div class="form-group form-full">
            <label for="file"
              >Upload Revised File <span class="required">*</span></label
            >
            <div class="file-input-wrapper">
              <label class="file-input-label" for="file"
                >Choose File (PDF only)</label
              >
              <input type="file" id="file" name="file" accept=".pdf" required />
              <div class="file-name">Only PDF files are allowed</div>
            </div>
          </div>

          <div class="form-actions">
            <a href="/student/assignments" class="btn btn-secondary">Cancel</a>
            <button type="submit" class="btn btn-primary">
              Resubmit Assignment
            </button>
          </div>
        </form>
      </div>
      <% } else { %>
      <div class="form-card">
        <p style="text-align: center; color: var(--muted-foreground)">
          Assignment not found
        </p>
      </div>
      <% } %>
    </div>

    <script>
      document.getElementById('file').addEventListener('change', function (e) {
        const fileName = document.querySelector('.file-name')
        if (e.target.files.length > 0) {
          fileName.textContent = e.target.files[0].name
        }
      })
    </script>
  </body>
</html>

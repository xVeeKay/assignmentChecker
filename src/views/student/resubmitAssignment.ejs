
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Resubmit Assignment</title>
    <style>
      * {
        box-sizing: border-box;
      }
      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
          'Helvetica Neue', Arial, sans-serif;
        background: linear-gradient(135deg, #f0faf8 0%, #f6fbf7 100%);
        color: #1f2937;
        margin: 0;
        padding: 28px 20px;
        min-height: 100vh;
      }
      .page {
        max-width: 720px;
        margin: 0 auto;
      }
      .header {
        display: flex;
        align-items: center;
        gap: 12px;
        margin-bottom: 24px;
      }
      .btn-back {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        background: none;
        border: none;
        color: var(--primary, #0f766e);
        font-weight: 700;
        font-size: 14px;
        cursor: pointer;
        text-decoration: none;
        padding: 0;
      }
      .btn-back:hover {
        text-decoration: underline;
      }
      h1 {
        margin: 0;
        font-size: 24px;
        font-weight: 700;
        color: #111827;
      }
      .card {
        background: #ffffff;
        border: 1px solid rgba(0, 0, 0, 0.05);
        padding: 24px;
        border-radius: 12px;
        box-shadow: 0 2px 12px rgba(16, 24, 40, 0.06);
      }
      .section {
        margin-bottom: 20px;
      }
      .section:last-child {
        margin-bottom: 0;
      }
      .section-title {
        font-weight: 700;
        font-size: 13px;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        color: #6b7280;
        margin-bottom: 10px;
      }
      .current-file {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 12px;
        background: #f9fafb;
        border-radius: 8px;
        border: 1px solid #e5e7eb;
      }
      .file-link {
        color: var(--primary, #0f766e);
        text-decoration: none;
        font-weight: 600;
        word-break: break-all;
      }
      .file-link:hover {
        text-decoration: underline;
      }
      .remarks-box {
        background: linear-gradient(135deg, #fef3f2 0%, #ffe4e1 100%);
        border-left: 4px solid #ef4444;
        padding: 14px;
        border-radius: 8px;
        color: #7f1d1d;
        line-height: 1.5;
        font-size: 14px;
      }
      label {
        display: block;
        font-weight: 700;
        font-size: 14px;
        margin-bottom: 8px;
        color: #111827;
      }
      textarea,
      input[type='file'] {
        width: 100%;
        padding: 10px 12px;
        border-radius: 8px;
        border: 1px solid #d1d5db;
        background: #f9fafb;
        color: #111827;
        font-family: inherit;
        font-size: 14px;
      }
      textarea {
        min-height: 120px;
        resize: vertical;
      }
      textarea:focus,
      input[type='file']:focus {
        outline: none;
        border-color: var(--primary, #0f766e);
        background: #fff;
        box-shadow: 0 0 0 3px rgba(15, 118, 110, 0.08);
      }
      input[type='file'] {
        padding: 8px;
      }
      .form-row {
        margin-bottom: 16px;
      }
      .form-row:last-child {
        margin-bottom: 0;
      }
      .actions {
        display: flex;
        gap: 10px;
        align-items: center;
        margin-top: 20px;
        padding-top: 16px;
        border-top: 1px solid #e5e7eb;
      }
      .btn {
        padding: 10px 16px;
        border-radius: 8px;
        font-weight: 700;
        font-size: 14px;
        cursor: pointer;
        border: none;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 6px;
        transition: all 0.2s ease;
      }
      .btn-primary {
        background: var(--primary, #0f766e);
        color: #fff;
        box-shadow: 0 2px 8px rgba(15, 118, 110, 0.15);
      }
      .btn-primary:hover {
        background: #0d5f58;
        box-shadow: 0 4px 12px rgba(15, 118, 110, 0.25);
        transform: translateY(-1px);
      }
      .btn-cancel {
        background: #f3f4f6;
        color: #1f2937;
        border: 1px solid #d1d5db;
      }
      .btn-cancel:hover {
        background: #e5e7eb;
      }
    </style>
  </head>
  <body>
    <div class="page">
      <div class="header">
        <a href="/student/assignments" class="btn-back">← Back</a>
        <h1>Resubmit Assignment</h1>
      </div>

      <div class="card">
        <% if (!assignment) { %>
        <p style="color: #6b7280">No assignment found.</p>
        <% } else { %> <% var fileName = assignment.filePath ?
        assignment.filePath.split(/[\\/]/).pop() : '' %>

        <!-- Current File Section -->
        <div class="section">
          <div class="section-title">📎 Current File</div>
          <div class="current-file">
            <% if (assignment.filePath) { %>
            <a
              class="file-link"
              href="/student/assignments/<%= assignment._id %>/download"
              ><%= fileName %></a
            >
            <% } else { %>
            <span style="color: #9ca3af">No file uploaded</span>
            <% } %>
          </div>
        </div>

        <!-- Rejection Remarks Section -->
        <% if (typeof remarks !== 'undefined' && remarks) { %>
        <div class="section">
          <div class="section-title">⚠️ Rejection Remarks</div>
          <div class="remarks-box"><%= remarks %></div>
        </div>
        <% }else{ %>
        <div class="section">
            <div class="section-title">⚠️ Rejection Remarks</div>
            <div class="remarks-box">No remarks</div>
        </div>
        <%}%>

        <!-- Form Section -->
        <form
          action="/student/assignments/<%= assignment._id %>/resubmit"
          method="POST"
          enctype="multipart/form-data"
        >
          <div class="form-row">
            <label for="description">Description</label>
            <textarea id="description" name="description">
<%= assignment.description || '' %></textarea
            >
          </div>

          <div class="form-row">
            <label for="file">Upload New File (Optional)</label>
            <input id="file" name="file" type="file" />
          </div>

          <div class="actions">
            <button type="submit" class="btn btn-primary">
              Submit Resubmission
            </button>
            <a href="/student/assignments" class="btn btn-cancel">Cancel</a>
          </div>
        </form>
        <% } %>
      </div>
    </div>
  </body>
</html>

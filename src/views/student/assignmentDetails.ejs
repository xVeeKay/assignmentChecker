<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Assignment Details - AssignmentChecker</title>
    <style>
      :root {
        --radius: 0.65rem;
        --background: oklch(1 0 0);
        --foreground: oklch(0.141 0.005 285.823);
        --card: oklch(1 0 0);
        --card-foreground: oklch(0.141 0.005 285.823);
        --primary: oklch(0.541 0.281 293.009);
        --primary-foreground: oklch(0.969 0.016 293.756);
        --secondary: oklch(0.967 0.001 286.375);
        --secondary-foreground: oklch(0.21 0.006 285.885);
        --muted: oklch(0.967 0.001 286.375);
        --muted-foreground: oklch(0.552 0.016 285.938);
        --destructive: oklch(0.577 0.245 27.325);
        --border: oklch(0.92 0.004 286.32);
        --chart-2: oklch(0.606 0.25 292.717);
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
          Oxygen, Ubuntu, Cantarell, sans-serif;
        background: var(--background);
        color: var(--foreground);
        line-height: 1.6;
      }

      .container {
        max-width: 900px;
        margin: 0 auto;
        padding: 40px 32px;
      }

      .back-link {
        display: inline-block;
        margin-bottom: 24px;
        color: var(--primary);
        text-decoration: none;
        font-weight: 600;
        font-size: 14px;
        transition: all 0.2s ease;
      }

      .back-link:hover {
        color: var(--chart-2);
      }

      .header {
        display: flex;
        justify-content: space-between;
        align-items: start;
        margin-bottom: 32px;
        gap: 20px;
      }

      .header h1 {
        font-size: 32px;
        font-weight: 700;
        color: var(--foreground);
        margin: 0;
      }

      .status-badge {
        display: inline-block;
        padding: 8px 16px;
        background: var(--chart-2);
        color: white;
        border-radius: 20px;
        font-size: 13px;
        font-weight: 600;
      }

      .details-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 20px;
        margin-bottom: 32px;
      }

      .info-card {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        padding: 20px;
      }

      .info-label {
        font-size: 12px;
        font-weight: 600;
        color: var(--muted-foreground);
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 6px;
      }

      .info-value {
        font-size: 16px;
        font-weight: 600;
        color: var(--foreground);
      }

      .section {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        padding: 24px;
        margin-bottom: 20px;
      }

      .section-title {
        font-size: 16px;
        font-weight: 700;
        color: var(--foreground);
        margin-bottom: 16px;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .section-title::before {
        content: '';
        width: 3px;
        height: 20px;
        background: var(--primary);
        border-radius: 1px;
      }

      .description {
        font-size: 14px;
        line-height: 1.8;
        color: var(--foreground);
        white-space: pre-wrap;
        word-break: break-word;
      }

      table {
        width: 100%;
        border-collapse: collapse;
      }

      thead {
        background: var(--secondary);
        border-bottom: 2px solid var(--border);
      }

      th {
        padding: 12px;
        text-align: left;
        font-weight: 600;
        font-size: 12px;
        color: var(--muted-foreground);
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }

      td {
        padding: 12px;
        border-bottom: 1px solid var(--border);
        font-size: 13px;
      }

      tr:last-child td {
        border-bottom: none;
      }

      tr:hover {
        background: var(--secondary);
      }

      .action-badge {
        display: inline-block;
        padding: 4px 8px;
        background: var(--primary);
        color: var(--primary-foreground);
        border-radius: 4px;
        font-size: 11px;
        font-weight: 600;
      }

      .file-link {
        color: var(--primary);
        text-decoration: none;
        font-weight: 600;
        transition: all 0.2s ease;
      }

      .file-link:hover {
        text-decoration: underline;
      }

      .actions {
        display: flex;
        gap: 12px;
        margin-top: 20px;
      }

      .btn {
        flex: 1;
        padding: 12px 20px;
        border-radius: var(--radius);
        border: none;
        font-weight: 600;
        font-size: 14px;
        cursor: pointer;
        transition: all 0.2s ease;
      }

      .btn-primary {
        background: var(--primary);
        color: var(--primary-foreground);
      }

      .btn-primary:hover {
        opacity: 0.9;
        transform: translateY(-1px);
      }

      .btn-secondary {
        background: var(--secondary);
        color: var(--secondary-foreground);
        border: 1px solid var(--border);
      }

      .btn-secondary:hover {
        background: var(--muted);
      }

      .empty-message {
        text-align: center;
        padding: 20px;
        color: var(--muted-foreground);
        font-size: 13px;
      }

      @media (max-width: 1024px) {
        .container {
          max-width: 100%;
          padding: 32px 24px;
        }
      }

      @media (max-width: 768px) {
        .container {
          padding: 20px 16px;
        }

        .header {
          flex-direction: column;
        }

        .header h1 {
          font-size: 24px;
        }

        .details-grid {
          grid-template-columns: 1fr;
          gap: 16px;
        }

        .actions {
          flex-direction: column;
        }

        .btn {
          width: 100%;
        }

        th,
        td {
          padding: 12px 8px;
          font-size: 12px;
        }

        .section-title {
          font-size: 14px;
        }
      }

      @media (max-width: 480px) {
        .container {
          padding: 16px 12px;
        }

        .back-link {
          font-size: 12px;
          margin-bottom: 16px;
        }

        .header {
          gap: 12px;
        }

        .header h1 {
          font-size: 20px;
        }

        .status-badge {
          padding: 6px 12px;
          font-size: 12px;
        }

        .info-card {
          padding: 16px;
        }

        .info-label {
          font-size: 10px;
        }

        .info-value {
          font-size: 14px;
        }

        .section {
          padding: 16px;
          margin-bottom: 16px;
        }

        th,
        td {
          padding: 10px 6px;
          font-size: 11px;
        }

        .action-badge {
          font-size: 10px;
          padding: 3px 6px;
        }

        .btn {
          padding: 10px 16px;
          font-size: 13px;
        }
      }
    </style>
  </head>
  <body>
    <%- include('_sidebar') %>
    <div class="container">

      <% if (assignment) { %>
      <div class="header">
        <h1><%= assignment.title %></h1>
        <% var st = (assignment.status||'draft').toString().toLowerCase(); %>
        <span class="status-badge"
          ><%= st.charAt(0).toUpperCase() + st.slice(1) %></span
        >
      </div>

      <div class="details-grid">
        <div class="info-card">
          <div class="info-label">Category</div>
          <div class="info-value"><%= assignment.category || 'N/A' %></div>
        </div>
        <div class="info-card">
          <div class="info-label">Faculty Reviewer</div>
          <div class="info-value">
            <%= (assignment.faculty && assignment.faculty.name) ?
            assignment.faculty.name : 'Not assigned' %>
          </div>
        </div>
        <div class="info-card">
          <div class="info-label">Submitted Date</div>
          <div class="info-value">
            <%= assignment.createdAt ? new
            Date(assignment.createdAt).toLocaleDateString() : 'N/A' %>
          </div>
        </div>
        <div class="info-card">
          <div class="info-label">Last Updated</div>
          <div class="info-value">
            <%= assignment.updatedAt ? new
            Date(assignment.updatedAt).toLocaleDateString() : 'N/A' %>
          </div>
        </div>
      </div>

      <div class="section">
        <h2 class="section-title">Description</h2>
        <div class="description">
          <%= assignment.description || 'No description provided' %>
        </div>
      </div>

      <div class="section">
        <h2 class="section-title">Review History</h2>
        <% if (Array.isArray(history) && history.length > 0) { %>
        <table>
          <thead>
            <tr>
              <th>Date</th>
              <th>Action</th>
              <th>By</th>
              <th>Remarks</th>
            </tr>
          </thead>
          <tbody>
            <% history.forEach(function(h){ %>
            <tr>
              <td>
                <%= h.date ? new Date(h.date).toLocaleDateString() : 'N/A' %>
              </td>
              <td><span class="action-badge"><%= h.action %></span></td>
              <td><%= (h.user && h.user.name) ? h.user.name : 'N/A' %></td>
              <td><%= h.remarks || '-' %></td>
            </tr>
            <% }) %>
          </tbody>
        </table>
        <% } else { %>
        <div class="empty-message">No review history yet</div>
        <% } %>
      </div>

      <div class="actions">
        <% if (assignment.status === 'draft') { %>
        <form
          method="POST"
          action="/student/assignments/<%= assignment._id %>/submit"
          style="flex: 1"
        >
          <button type="submit" class="btn btn-primary" style="width: 100%">
            Submit for Review
          </button>
        </form>
        <% } %> <% if (assignment.status === 'rejected') { %>
        <a
          href="/student/assignments/<%= assignment._id %>/resubmit"
          class="btn btn-primary"
          style="text-decoration: none"
          >Resubmit Assignment</a
        >
        <% } %>
      </div>
      <% } else { %>
      <div class="empty-message">Assignment not found</div>
      <% } %>
    </div>
  </body>
</html>
